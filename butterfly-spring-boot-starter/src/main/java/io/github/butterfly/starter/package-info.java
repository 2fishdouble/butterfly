/**
 * Butterfly Spring Boot Starter entry coordinates.
 *
 * <p>This module contains no business logic; it only aggregates the
 * butterfly-spring-boot-autoconfigure dependency. The package-info file exists so
 * Maven Central can resolve sources/javadoc artifacts (required for every publish).
 */
package io.github.butterfly.starter;
